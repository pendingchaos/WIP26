include stdlib;

attribute pos:vec3;
attribute vel:vec3;
attribute col:vec3;
attribute time:float;

var i:float = 0;

while i < 100 {
    pos = vec3(0);
    vel = normalize(vec3(1, i/50-1, 1)) * vec3(0.005);
    col = vec3(1);
    time = 0;
    emit();
    i = i + 1;
}
